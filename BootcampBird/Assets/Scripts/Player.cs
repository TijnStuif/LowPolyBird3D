using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player : MonoBehaviour
{
    Rigidbody rb;
    // Start is called before the first frame update
    void Start()
    {
        transform = new Vector3(0, 20, 0);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
